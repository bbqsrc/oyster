'use strict';

describe('User management API', function() {
  xit('can only be accessed by admins', function() {

  });

  xit('only allows superadmins to change user details', function() {

  });

  xit('does not allow a superadmin to delete themselves', function() {

  });

  xit('does not allow a superadmin to demote themselves', function() {

  });

  xit('allows a superadmin to remove a user', function() {

  });

  xit('allows a superadmin to change the role of a user', function() {

  });

  xit('allows an admin to reset the password of another user', function() {

  });
});
